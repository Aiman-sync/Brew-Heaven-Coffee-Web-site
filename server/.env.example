# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Connection
# Get this from MongoDB Atlas (free tier available)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/brew-haven

# Frontend URL (for CORS)
# Replace with your Vercel URL after deploying frontend
FRONTEND_URL=https://your-app-name.vercel.app
